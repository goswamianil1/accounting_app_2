[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'

[tool.flake8]
max-line-length = 88
extend-ignore = ['E203']
exclude = [
    '.git',
    '__pycache__',
    'build',
    'dist',
    'venv',
    'migrations'
] 